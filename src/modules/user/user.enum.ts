export enum UserStatus {
  IN_PROGRESS = 'in-progress',
  BLOCKED = 'blocked',
}

export enum UserAuthSource {
  EMAIL = 'email',
  GOOGLE = 'google',
}

export enum UserRole {
  ADMIN = 'admin',
  EDITOR = 'editor',
}
